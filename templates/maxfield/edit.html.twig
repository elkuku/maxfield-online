{% extends 'base.html.twig' %}

{% block title %}
Edit
{% endblock %}

{% block body %}
    <div class="container">
        <div class="float-end">
            <a href="{{ path('maxfield_delete', {'id': maxfield.id})}}" class="btn btn-outline-danger">Delete</a>
            <a href="{{ path('default') }}" class="btn btn-outline-secondary">Back</a>
        </div>
    <div class="row">
        <div class="col-xs-12">
            <h1>Edit Maxfield</h1>
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" class="btn btn-primary">Save</button>
            {{ form_end(form) }}
        </div>
    </div>
    </div>
{% endblock %}

